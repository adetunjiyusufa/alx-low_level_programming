#include" main.h"

/**
 * is_prime_number - to check if is a prime number
 *
 * @n: interger to check
 *
 * Return: int
 *
 */

int is_prime_number(int n)
{
	int i, j;

	if (if j < 2 || j % i == 0)
		return (0);
	else if (i > j / 2)
		return (1);
	else
		return (check(2, n));
}
